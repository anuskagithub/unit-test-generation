task: "Generate C++ unit tests"

requirements:
  - Use only assert-style tests inside a global int main() function
  - Do not use any function that is not declared in sample.hpp
  - Include <cassert> and "sample.hpp" headers
  - Do not define any functions like add() or subtract()

formatting:
  - Use consistent indentation (4 spaces)
  - Avoid unnecessary whitespace
  - Comment each test with what it’s testing

output:
  - Provide only the test code, no explanation or markdown formatting
  - Do not include triple backticks (```)
  - Ensure the test compiles successfully

language: "C++"
